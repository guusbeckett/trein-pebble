`config.html` is a standalone development copy of the configuration page. The
app bundles its production configuration page in `src/pkjs/config_page.js`, so
settings do not depend on an externally hosted website.
